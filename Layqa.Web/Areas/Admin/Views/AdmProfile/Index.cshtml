@model IEnumerable<Layqa.Web.Areas.Admin.Models.AdmProfileListViewModel>
@{
    ViewBag.Title = "Perfiles del Administrador";
}

<!-- Page Heading -->
<div class="row">
    <div class="col-lg-12">

        <h2>@ViewBag.Title</h2>

        <p>
            @Html.ActionLink("Crear Perfil", "Create", null, new { @class = "btn btn-success" })
        </p>
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.ValidationMessage("Error", new { @class = "text-danger" })
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        <a href="@Url.Action("Edit", new { Id = item.AdmProfileId })" class="btn btn-warning btn-sm"><i class="fa fa-fw fa-edit"></i> Editar</a>
                        <a href="@Url.Action("Delete", new { Id = item.AdmProfileId })" class="btn btn-danger btn-sm" data-ajax="true" data-ajax-method="Get" data-ajax-mode="replace" data-ajax-update="#DeleteForm" data-target="#DeleteForm" data-toggle="modal"><i class="fa fa-fw fa-trash"></i> Eliminar</a>
                    </td>
                </tr>
            }

        </table>

        <div id="DeleteForm"
             class="modal fade"
             tabindex="-1"
             role="dialog"
             aria-hidden="true">
        </div>
    </div>
</div>
<!-- /.row -->

@section Scripts{
    @Scripts.Render("~/bundles/unobtrusiveajax")
}
