@model IEnumerable<Layqa.Web.Areas.Admin.Models.AdmUserListViewModel>

@{
    ViewBag.Title = "Administradores";
}

<!-- Page Heading -->
<div class="row">
    <div class="col-lg-12">

        <h2>@ViewBag.Title</h2>

        <p>
            @Html.ActionLink("Crear Administrador", "Create", null, new { @class = "btn btn-success" })
        </p>
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.ValidationMessage("Error", new { @class = "text-danger" })
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.FullName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Active)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Email)
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.FullName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Active)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Email)
                    </td>
                    <td>
                        <a href="@Url.Action("Edit", new { Id = item.UserId })" class="btn btn-warning btn-sm"><i class="fa fa-fw fa-edit"></i> Editar</a>
                        <a href="@Url.Action("Delete", new { Id = item.UserId })" class="btn btn-danger btn-sm" data-ajax="true" data-ajax-method="Get" data-ajax-mode="replace" data-ajax-update="#DeleteForm" data-target="#DeleteForm" data-toggle="modal"><i class="fa fa-fw fa-trash"></i> Eliminar</a>
                    </td>
                </tr>
            }

        </table>

        <div id="DeleteForm"
             class="modal fade"
             tabindex="-1"
             role="dialog"
             aria-hidden="true">
        </div>
    </div>
</div>
<!-- /.row -->

@section Scripts{
    @Scripts.Render("~/bundles/unobtrusiveajax")
}
